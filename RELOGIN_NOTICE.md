# 再ログイン案内 ─ 文面と送信計画

作成: 2026-08-01 / **状態: 文面のみ。送信は一切していない**

`{切替日}` `{期限}` は承認後に確定する。文面中の日付はすべてこの2つだけ。

---

## 1. 送信対象の実測（2026-08-01 07:40）

| 区分 | 人数 | 備考 |
|---|---:|---|
| 登録総数 | 38 | is_active 38 |
| **active cohort（直近7日に記録）** | **15** | 切り替えの成否を決めるのはこの人たち |
| LINEで届く | 21 | |
| LINE未連携 | 17 | |
| メール形式が有効 | 38 | ログインに使っているアドレスなので原則到達する |
| **どちらでも届かない** | **0** | |
| アプリ内表示の対象 | 36 | セッション未取得者 |

### 最重要の発見

**active cohort 15名は、全員がLINE連携済み。**

つまり **LINEだけで、切り替えに関係する人の100%へ届く**。
LINE未連携の17名は、全員が直近7日に1件も記録していない。

この17名にメールを送るかどうかは、性質が違う判断になる:

- 送る利点: 久しぶりに開いたときに戸惑わない
- 送る欠点: **半年使っていない人にも通知が飛ぶ**。休眠者を掘り起こす行為であり、
  「セキュリティ強化のお知らせ」として届くと不安を与えかねない

**推奨: 17名へのメールは送らない。** アプリ内表示だけで十分。
彼らが次に開いたときに再ログイン画面が出れば目的は達する。
ChatGPTの方針「非アクティブユーザーは次回アプリを開いた際に再ログインさせる」とも一致する。

送る場合は §4 の文面を使う。

---

## 2. LINE用（21名 / うち cohort 15名）

短く、行動だけを伝える。内部事情は書かない。

```
【JIROKU】ログインのやり直しをお願いします

いつもありがとうございます。

セキュリティを強化したため、
お手数ですが一度ログインし直してください。

▼ やること（30秒）
アプリを開く → Googleでログイン
これだけです。

▼ ご安心ください
・記録、目標、これまでのデータは消えません
・入力中で保存できていない記録も、
　ログインすれば自動で反映されます

▼ 期限
{期限} までにお願いします。
{切替日} 以降は、ログインし直さないと
記録の保存ができなくなります。

▼ アプリ
https://kaisunagawa.github.io/edventure-app/

うまくいかないときは、このトークに
「ログインできない」と送ってください。
確認します。
```

**書かなかったこと**と理由:

- 「脆弱性がありました」→ 利用者が取る行動は変わらないのに不安だけ残る
- 「他人にデータを見られる可能性がありました」→ 事実として確認されていない。
  推測で不安を与えるべきではない
- 技術用語（セッション、トークン、認証基盤）→ 一切使わない

**「ログインできない」と送ってもらう導線**を入れたのは、
今の連携済みユーザーは既にLINEでこちらへ送れる状態にあり、
新しい連絡先を覚えてもらう必要がないため。

---

## 3. アプリ内表示（36名 / セッション未取得者のみ）

既存の `ReauthGate` を拡張する。**未実装・未デプロイ**（デプロイ＝全員に表示されるため）。

### 表示条件

```
セッションを持っていない  かつ  auth_mode が LEGACY でない
```

セッションを持っている人には**出さない**。

### 文面

```
┌────────────────────────────────┐
│              JIROKU                     │
│                                         │
│   安全のため、一度ログインし直してください   │
│                                         │
│   ログインの仕組みを新しくしました。         │
│   お手数ですが、もう一度Googleで           │
│   ログインしてください。                   │
│                                         │
│   記録・目標・設定はそのまま残ります。       │
│                                         │
│   ⚠ 保存できていない記録が◯件あります。     │
│      ログインすると自動で反映されます。       │
│      （0件のときは出さない）                │
│                                         │
│   {切替日} 以降は、ログインし直さないと      │
│   記録の保存ができなくなります。             │
│   期限: {期限}                           │
│                                         │
│      [  ログイン画面へ  ]                 │
│                                         │
│      あとで（今日はこのまま使う）           │
└────────────────────────────────┘
```

### 「あとで」の扱い

- **押せるのは切替日まで。** 切替日以降は選択肢を出さない
  （出しても保存できないので、期待を持たせるだけになる）
- 押した場合は**その日のうちは再表示しない**（1日1回まで）
- 押しても読み取りは従来どおり使える。書けなくなるのは切替日から

**「あとで」を用意する理由**: 記録は「思い出した瞬間」に入れるもの。
そこでログインを強制すると、記録そのものを諦める人が出る。
記録を止めないことを優先する。

---

## 4. メール（LINE未連携17名 / **推奨は送らない**）

送る場合の文面。GASの `MailApp` で送信可能（1日100通まで。17通は問題ない）。

```
件名: 【JIROKU】ログインのやり直しのお願い

いつもJIROKUをご利用いただきありがとうございます。

セキュリティ強化のため、お手数ですが
一度ログインし直していただくようお願いいたします。

■ やること
下記からアプリを開き、Googleでログインするだけです。
https://kaisunagawa.github.io/edventure-app/

■ データについて
これまでの記録・目標・設定は消えません。
保存できていない入力も、ログイン後に自動で反映されます。

■ 期限
{期限} までにお願いします。
{切替日} 以降は、ログインし直さないと記録の保存ができません。

うまくいかない場合は、このメールにご返信ください。

JIROKU
```

---

## 5. 重複送信の扱い

同じ人に**LINEとメールの両方は送らない**。

```
LINE連携あり(21名) → LINEのみ
LINE未連携(17名)   → 送らない（推奨）／送るならメールのみ
全員(36名)         → アプリ内表示（重複ではない。開いたときだけ見える）
```

アプリ内表示はLINEと重なるが、これは重複と見なさない。
LINEは「開いてもらう」ため、アプリ内は「開いた人に何をすべきか示す」ためで、
役割が違う。

**送信は1回だけ。** 追い打ちのリマインドは、切替日の前日に
**まだセッションを取得していないcohortメンバーだけ**へ絞って送る。
全員に再送すると、既に対応した人に「やっていない」と伝わってしまう。

---

## 6. 到達状況の確認方法

送信後は次で追う。

```bash
bash gas/ops.sh authCohort days=7
```

見る数字:

| 指標 | 意味 |
|---|---|
| `cohortSize` | 母数（直近7日に記録した人） |
| `cohortWithSession` | 案内後にログインし直した人 |
| `login24h.realFlowUnresolved` | **0でなければ切り替えない** |
| `login24h.byFingerprint` | 誰か特定の人だけが失敗し続けていないか |
| `reach.inAppTargets` | アプリ内表示が出る人数（減っていけば進捗） |

**LINEの既読は取れない。** 到達の証拠になるのはセッション取得だけなので、
「送った人数」ではなく「セッションを取得した人数」で進捗を見る。

送信後に追加で必要な計測（ChatGPTの指示分）:

- 案内後にアプリを開いた人数 → `Users.last_seen` 相当が無いため
  **現状では取れない**。取るには列の追加が要る（未実施）
- 未保存操作の再試行件数・同期失敗件数 → クライアント側の数字なので
  **サーバーには届いていない**。取るには送信の実装が要る（未実施）

この2つは**現時点で計測できない**。必要なら実装するが、
どちらも新しいデータ収集にあたるため承認が要る。

---

## 7. 送信手順（承認後に実行）

```bash
# 1. 対象と件数を最終確認
bash gas/ops.sh authCohort days=7

# 2. LINE一斉送信（21名）※ 文面はコード側に置く
bash gas/ops.sh adminBroadcastLine dryRun=1   # まず本文だけ確認
bash gas/ops.sh adminBroadcastLine            # 実送信

# 3. 30分後に到達を確認
bash gas/ops.sh authCohort days=7
```

**dryRun=1 で本文を必ず目視してから送る。**
一斉送信は取り消せない。
