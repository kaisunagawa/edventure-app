# 認証導入計画（P0）

作成: 2026-07-31 / **最終更新: 2026-08-01（大半を実装済み）**

> ⚠️ この文書は計画として書き始めたもので、以降に「実装は未着手」という記述が
> 残っているが、**それらは古い**。現在地は下の「現在地と次の一手」を見ること。

---

# 現在地と次の一手（2026-08-01 05:00 時点）

## 稼働中

| 項目 | 状態 |
|---|---|
| 本番デプロイ | @413 / `auth_mode = SESSION_OPTIONAL` |
| Google IDトークン検証＋自前セッション | ✅ 稼働（PC/スマホ/LINE内ブラウザで確認済み） |
| 高リスク54件（admin*/coach*）の認証・認可 | ✅ 稼働（全数実測で拒否を確認） |
| 台帳未登録でも既定で保護（fail-closed） | ✅ |
| LINE連携のワンタイムコード化 | ✅（メール連携は廃止） |
| コーチCRMのIDトークン方式 | ✅（縮退あり） |
| 運用リクエストの署名方式 | ✅（`?secret=` の旧方式は廃止） |
| 週次バックアップ | ✅ 毎週日曜3時・トリガー登録済み |
| **本人の書き込み/読み取りAPIの認証必須化** | ⏸ **コードは投入済み・スイッチOFF** |

## 次の一手 ①：CP3/CP4のスイッチON

### 判断基準（2026-08-01 改訂）

**「usableUsersが8名を超えたら」という固定人数は廃止した。**
利用者が増減すると意味が変わってしまうため。代わりに
**「直近7日に実際に記録した人（active cohort）」を母数**にする。

```bash
cd ~/Projects/edventure-app
export P1_ADMIN_SECRET='...'
bash gas/ops.sh authCohort days=7
```

### WRITE必須化のGo条件（全部満たしたらGo）

| # | 条件 | 2026-08-01 07:10 実測 |
|---|---|---|
| 1 | 再ログインの案内を対象利用者へ送信済み | ❌ **未送信**（Kaiの承認待ち） |
| 2 | active cohort を算出できている | ✅ 15名 |
| 3 | 案内後にアプリを開いた cohort 全員がセッション取得済み | ❌ 2/15（13%）※案内前なので当然 |
| 4 | 直近24時間に未解決のログイン失敗が無い | ✅ **0件**（実フロー由来） |
| 5 | PC・スマホ・LINE内ブラウザで保存の再試行が成功 | ⏸ 2026-07-31に確認済み。切替直前に再確認 |
| 6 | オフライン記録が認証後に本人へ同期される | ⏸ 実装済み・切替直前に実機確認 |
| 7 | 他人のstudentEmailを送っても本人へ強制される | ⚠ **セッション込みの実測ができていない**（後述） |
| 8 | 他人のIDで読み取り・更新・削除ができない | ⚠ 同上 |
| 9 | 即時ロールバック手順が実測済み | ⏸ 切替時にセットで実測する |
| 10 | 切り替え日時を事前告知済み | ❌ 未告知 |

**現時点は No-Go。** 止まっているのは技術ではなく ①案内の送信 ②告知。
どちらもKaiの承認が要る。

### 条件7・8が「⚠」である理由

有効なセッションを持った状態でのテストが**再現できない**。
セッショントークンは平文を保存していないので、テストのたびに
実際にGoogleログインを踏む必要がある。2026-07-31にはKaiのログイン中に
手作業で確認したが、**自動テストとして繰り返せない**。

繰り返せるようにするには「テスト用セッションを発行する口」が要るが、
それは**認可境界の変更**にあたるため、勝手には作らない。
案としては、発行したセッションに `TESTONLY` の印を付け、
本番デプロイでは `verifySession` が必ず拒否する形。要承認。

### READ必須化のGo条件

WRITE必須化後、**最低24時間**の安定運用を確認したうえで、
以下が他人のメール・IDで取得できないことを実測する。

`getLogs` / `getReportList` / `getHomeData` / `getGoalTree` /
Reports / Journal / Goals / WeeklyGoals / Tasks

### 切り替えコマンド

```bash
bash gas/ops.sh authSetEnforce kind=WRITE on=1   # 書き込みを必須化
bash gas/ops.sh authSetEnforce kind=READ  on=1   # その後、読み取りも
bash gas/ops.sh authSetEnforce kind=WRITE on=0   # 戻すとき
```

**デプロイ不要で即時反映・即時ロールバック。** 切り替え直後は
`bash gas/ops.sh adminOpsHealthCheck dryRun=1` で記録件数を見て、
急に0にならないかを確認すること。

### 候補日時（案内送信日を D とする）

| 段階 | 時期 | 根拠 |
|---|---|---|
| 再ログイン案内の送信 | **D = 承認当日** | ここが起点。送るまで何も動かない |
| WRITE必須化 | **D+3日 の平日10:00** | 3日あれば記録している15名の大半がアプリを開く。朝は記録が少なく、失敗しても被害が小さい |
| READ必須化 | **WRITE+24時間以上（D+4日 10:00）** | 24時間の安定運用を確認してから |
| SESSION_REQUIRED | **READ+7日（D+11日）** | 週1しか開かない人を1周ぶん待つ |

**D=8月2日（日）に承認された場合の具体日**

| 段階 | 日時 |
|---|---|
| 案内送信 | 8月2日（日） |
| WRITE必須化 | **8月5日（水）10:00** |
| READ必須化 | **8月6日（木）10:00** |
| SESSION_REQUIRED | **8月13日（木）10:00** |

**互換モードの期限**: SESSION_REQUIRED まで到達した時点で
`SESSION_OPTIONAL` は廃止する。無期限には残さない。
**上限を 2026年8月31日** とし、それまでに到達しない場合は
原因を報告したうえで期限を切り直す。

## 次の一手 ②：LINE Webhookの署名検証（P1）

**GASはリクエストヘッダーを取得できない**（実測確認済み）ため、
`x-line-signature` の検証は原理的に不可能。恒久対応として
**Cloud Run のセキュリティゲートウェイを採用**することが決定した。

設計の全文は [CLOUDRUN_GATEWAY.md](CLOUDRUN_GATEWAY.md)。
要点は次のとおり。

- Node.js 22 / TypeScript / Hono。raw bodyを変更せず取得できることを重視
- `/line/webhook`（LINE署名検証・冪等・GASへ転送）と
  `/auth/verify`（IDトークンを公式ライブラリで検証）の2つだけ
- 鍵は用途別に分離。Secret Manager でボリュームマウント
- **月額 約60円**（Cloud Armor を入れる場合は約1,000円）
- 工数 約6.5日＋観測1週間
- **CP3/CP4と同じ週にやらない**（どちらも「使えない」形で失敗するため切り分け不能になる）

暫定のワンタイムコード方式は、移行完了まで維持する。

## 残るP1

- LINE WebhookのCloud Run移行
- IDトークン検証を tokeninfo から公式ライブラリへ
- CP3/CP4の認証強制
- ID指定型APIの所有権回帰テスト（セッション込みの自動化）
- `line_user_id` 変更履歴の監査（**過去分は遡れない**）

## 残るP2

- `loginWithAccessToken` とサーバー側 `loginAccess` の削除
  （コーチCRMのIDトークン方式が安定したら）
- 共有署名鍵の専用サービス主体化（現在は鍵1本）
- AI費用の機能別分類
- CSPと外部スクリプトのセルフホスト化検討
- 500人以降の項目：利用規約・プライバシーポリシーの公開、外部脆弱性診断、
  個人情報保護法の安全管理措置の文書化

## 将来仕様として保持（まだ実装しない）

タスクの重要度・緊急度。認証の切り替え完了後、Checkpoint 3 の設計項目として扱う。

`importance_level` / `due_at` / `urgency_level` / 重要度×緊急度の4分類 /
期限超過率 / 重要タスク完了率 / 緊急化する前に完了できた割合

---


## 1. 何が起きているか

このWeb appは**リクエストに書かれたメールアドレスをそのまま信用している**。
`doGet` は `e.parameter.studentEmail`、`doPost` は `body.studentEmail` を読むだけで、
トークン検証も `Session.getActiveUser()` も無い。

実測（2026-07-31）: 認証情報を持たない curl から `coachEmail` に管理者のアドレスを
書いただけで、**在籍生徒21名の氏名とメールアドレスを取得できた**。

### 公開状況

| 区分 | 件数 |
|---|---:|
| doGet/doPost で公開しているアクション | **143** |
| うち鍵（P1_ADMIN_SECRET）で保護済み | **5** |
| coach* アクション | 28 |
| admin* アクション | 25 |

`verifyAdmin()` / `verifyCoach()` は存在するが、**引数がクライアント由来の文字列**なので
認可として機能していない。

### 最も危険なもの（被害の大きさ順）

| アクション | 何ができてしまうか |
|---|---|
| `adminBroadcastLine` | **全利用者へLINEを一斉送信** |
| `adminSendStudentCampaign` | **全利用者へ一斉メッセージ** |
| `coachSendStudentMessage` | コーチを装って個別にメッセージ送信 |
| `coachGetStudentDetail` | 特定個人の氏名・記録・コーチング記録を全取得 |
| `coachGetStudents` | 全生徒の一覧と連絡先（**実測で取得成功**） |
| `adminTagCohortByEmails` 他 | 利用者データの一括書き換え |
| `getLogs` / `getReportList` / `getHomeData` | 任意個人の行動記録・日記・AI所見 |
| `coachUploadFile` / `coachDeleteFile` | 契約書ファイルの追加・削除 |

**評価**: 読み取りだけでなく**送信と破壊**ができる。人数ではなくデータの機密性で
判断すべきというレビュー指摘は妥当。

### Checkpoint 2/3 で追加したAPIの位置づけ

`saveGoal` / `saveWeeklyGoal` / `archiveGoalItem` / `getGoalTree` / `migrateLocalTasks` は
`goals_v1` フラグ必須で、現在の対象は**Kai 1人だけ**。

これらを止めても**リスクはほぼ減らない**。攻撃者がKaiを騙れる時点で、
上の表にある既存APIの方が桁違いに危険だから。
一方でKaiは既に実データの3か月目標を入れて使い始めている。
→ **止める実益がなく、使用中の機能を壊すだけなので、停止は推奨しない。**
本当に減らすべきは既存の高リスクAPIの露出。

### アクセスログの確認について

**確認できていない。** `clasp logs` はGCPプロジェクト未設定で使用不可。
Apps Scriptエディタの「実行数」画面からは手動で確認できるが、
匿名Webアプリの呼び出しは呼び出し元を識別できず、保持期間も限られる。
**「不正アクセスが無かった」とは言えない**ことを明記しておく。

---

## 2. 認証の現実解 ─ すでに8割できている

**重要な発見**: このアプリは**すでに Google ログインを実装している**。

`index.html` は Google Identity Services で `response_type=id_token` を要求し、
署名付きの `id_token`（JWT）を受け取っている。ところが `parseJwt()` で
**クライアント側で中身を読み、メールアドレスの文字列だけ取り出して捨てている**。
サーバーはこのトークンを一度も見ていない。

```
現在: [Google] --id_token--> [ブラウザ] --email文字列だけ--> [GAS]  ← 誰でも詐称できる
あるべき: [Google] --id_token--> [ブラウザ] --id_token--> [GAS] --検証--> email確定
```

つまり**本人確認の仕組みは既にあり、それをサーバーまで運んでいないだけ**。
ゼロから認証基盤を作る話ではない。

### 方式の比較

| 方式 | 実装量 | LINE内ブラウザ | 評価 |
|---|---|---|---|
| **Google ID token 検証 ＋ 自前セッション** | **小** | ○（現在も動作中） | **推奨** |
| メールのワンタイムコード | 中 | ○ | 既存Googleログインと二重になる |
| マジックリンク | 中 | △ 別ブラウザで開くと切れる | 不採用 |
| LINE連携による本人確認 | 中 | ○ | LINE未連携者が使えない |
| 署名付きセッションのみ（本人確認なし） | 小 | ○ | 最初の本人確認が無いので不可 |

**推奨: Google ID token を検証 → サーバー発行の短命セッショントークンへ交換。**
理由は ①既存のログイン導線をそのまま使える ②利用者の操作が増えない
③LINE内ブラウザで既に動作実績がある ④追加の外部サービスが不要。

### 検証のしかた（GASでの現実的な実装）

GASにはRSA署名の**検証**関数が無いため、JWTをローカル検証するのは面倒。
`https://oauth2.googleapis.com/tokeninfo?id_token=...` へ問い合わせる方式にする。
毎回叩くと遅いので、**初回だけ検証してセッショントークンを発行**する。

```
POST action=login  { idToken }
  → tokeninfo で検証（aud が自分の client_id か / exp / email_verified）
  → セッショントークン（乱数32バイト）を発行して返す
  → サーバーには sha256 ハッシュだけ保存（平文は保存もログ出力もしない）

以降の全リクエスト  { token }
  → ハッシュで引いて email / role / 有効期限 / token_version を確定
  → studentEmail・coachEmail のクライアント入力は一切信用しない
```

### Kai限定の暫定方式（Phase 1検証中）

正式版まで待たずに、Kaiのアカウントだけ先に閉じることができる。
上の仕組みの「ログイン」部分を省き、**サーバーで1本だけトークンを発行**して
Kaiの端末に一度だけ設定する。

- 発行はエディタ実行の関数で行い、**index.html には絶対に埋め込まない**
- ScriptProperties にはハッシュのみ保存
- 有効期限あり・`token_version` を上げれば即失効
- 平文はログに出さない

ただし**これはKai個人しか守らない**。生徒21名の情報が露出している状況は変わらないので、
暫定方式に時間をかけるより正式版を早く出す方が良いと考える。

---

## 3. ロールとスコープ

```
USER         自分のデータのみ
COACH        担当している利用者の COACH_REVIEW 範囲のみ
MANAGER      許可された MANAGER_REPORT のみ
ORG_ADMIN    組織設定と ORG_AGGREGATE
JIROKU_ADMIN 運営に必要な管理権限
```

公開レベル（[PHASE1_BASELINE.md](PHASE1_BASELINE.md) と共通）:

```
PRIVATE         本人のみ。生の記録・感情・健康・自由記述・AI会話・失敗内容
COACH_REVIEW    運営／担当コーチ
MANAGER_REPORT  上司。抽出された業務レポートのみ
ORG_AGGREGATE   組織管理者。個人を特定しない集計値
```

**MANAGER と ORG_ADMIN は PRIVATE の生ログを取得できない。**
ロールはトークンから確定し、クライアントが送る `role` は根拠にしない。

トークンから確定する項目: `user_id` / `student_email` / `role` /
`organization_id` / `coach権限` / `有効期限` / `token_version`

---

## 4. 既存24人の移行

利用者の操作は**「もう一度ログインするだけ」**。
ログイン方法自体は今までと同じGoogleログインなので、新しく覚えることは無い。

1. サーバー側にトークン検証を入れる（この時点ではまだ強制しない）
2. フロントを更新し、ログイン時にトークンを取得・保存して全リクエストに付ける
3. 数日、**どのリクエストがトークン付きか**を記録して到達率を見る
4. 100%に達したら、トークン必須へ切り替え
5. 古いセッションは無効化され、次回起動時に自動でログイン画面へ

**旧デプロイの無効化**: 認証導入後、過去のデプロイURLが残っていると
そこから認証なしで叩けてしまう。`clasp deployments` で一覧を出し、
本番・テスト以外は `clasp undeploy` で削除する。テスト用デプロイも
同じ認証を通す（素通しの入口を残さない）。

---

## 5. 見積もりとリスク

| 作業 | 目安 |
|---|---|
| サーバー: トークン発行・検証・doGet/doPost の入口改修 | 0.5日 |
| サーバー: coach/admin 系の認可をロール判定へ置き換え | 0.5日 |
| フロント: ログイン時のトークン取得と全リクエストへの付与 | 0.5日 |
| 到達率の観測と切り替え | 0.5日（実時間は数日） |
| 合計 | **約2日＋観測期間** |

入口が `doGet`/`doPost` の2箇所に集約されており、ほとんどのアクションが
`studentEmail` を引数で受け取る形になっているため、**143個を個別に直す必要はない**。
入口で確定した本人を流し込めば大半が片付く。

### リスク

| リスク | 対策 |
|---|---|
| 全員がログインできなくなる | テスト用デプロイで先に検証。段階的に切り替え。revert手順は整備済み |
| tokeninfo の呼び出しで遅くなる | ログイン時のみ。以降は自前トークンで、外部通信なし |
| LINE内ブラウザでトークンを保持できない | 現在も localStorage でログイン状態を保持できているので同条件 |
| 切り替え中の隙間 | 高リスクAPI（送信・一括操作）から先に必須化する |

---

## 6. 自動バックアップ（設計のみ）

```
毎週1回（日曜 03:00）トリガー
  SpreadsheetApp.copy() で複製          ← Drive権限の承認が不要
  シート数と主要件数を元ファイルと突き合わせ
  getEditors/getViewers で外部公開が無いことを確認
  8週間より古いバックアップを削除
  結果をLINEで管理者へ通知（成功・失敗とも）
```

**安全装置（必須）**:
- 削除対象は名前が `JIROKU_backup_` で始まるものだけ
- **本番のスプレッドシートIDと一致するものは、いかなる場合も削除しない**（ID照合を先に行う）
- 1回の実行で削除できるのは最大3件（暴走時の被害を限定）
- 削除前に必ず件数を記録し、通知に含める

**復元手順**: バックアップのコピーを作る → 新しいコピーのIDを `SPREADSHEET_ID` に設定
→ `bash gas/push_gas.sh` → スモークテストで確認。
元ファイルを直接編集して戻さない（失敗したときに戻る場所が無くなるため）。

---

## 7. Checkpoint 3 の集計仕様（再開前に直す）

レビュー指摘を受けた修正点。**まだ直していない。**

### metric_type の命名を統一する

現在 `count` / `minutes` / `boolean`（小文字）。
→ **`COUNT` / `DURATION` / `DAYS` / `BOOLEAN`** に統一する。
`minutes` と `duration` の混在をやめる。
※既存データは検証用を含め全て削除済みなので、**移行は不要**。

### COUNT

現在は quantity 未入力を 1 として加算している。
→ **正の数の quantity がある時だけ加算する。**

*補足*: これは正しいが、記録しても進捗が 0 のままだと「壊れている」と感じられる。
UI側で、COUNT の週間目標に紐づけた時は**数量入力を必須にする**必要がある。

### DURATION

現在は time_block 全体を自動で実績にしている。
→ 優先順位を **①ユーザーが入力した quantity ②完了タスクの actual_minutes
③どちらも無ければ加算しない** に変更する。

*補足*: ただし「その記録をこの週間目標に紐づける」という操作自体が、
**その時間枠をその目標に使ったという本人の明示**にあたる。
紐づけがある場合に time_block から算出するのは、レビューが認めた例外に該当すると考える。
毎回分数を手入力させると記録の手間が増え、記録すること自体をやめてしまう。
→ **紐づけ済みの場合のみ time_block を採用**する案で合意を取りたい。

### BOOLEAN

本人が週間目標を明示的に完了にした時だけ 1。**現状のままでよい。**

### DAYS

有効な `primary_weekly_goal_id` を持つログの日付を重複なしで数える。**未実装。**

---

## 8. 次にやること

1. この計画へのKaiの承認
2. 高リスクAPI（送信系・一括操作系）を先に封じる
3. Google ID token 検証とセッション発行の実装
4. 段階的な切り替え
5. 自動バックアップ
6. 集計仕様を直して Checkpoint 3 を再開

---
---

# 最終実装計画（2026-07-31 追記）

レビュー指摘を反映。**実装は未着手。**

## Auth Checkpoint 0：現状確認 ─ 完了

| 項目 | 実測結果 |
|---|---|
| 読み込んでいるライブラリ | `https://accounts.google.com/gsi/client`（**Google Identity Services＝現行版**） |
| 非推奨ライブラリ | `gapi.auth2` / `platform.js` / `g-signin2` は**1件も無い。移行不要** |
| OAuthクライアントID | `476804060858-n3afj7ipmc81vq8cq71u0u3jdpolshea.apps.googleusercontent.com` |
| ログイン方式 | `accounts.google.com/o/oauth2/v2/auth` へリダイレクト（implicit flow） |
| response_type | `id_token` |
| scope | `openid email profile` |
| redirect_uri | `https://kaisunagawa.github.io/edventure-app/` |
| コールバック | `location.hash` から `id_token` を取り出し `handleGoogleLogin()` へ |
| 許可済みJavaScript生成元 | **未確認**（GCPコンソールを見る必要がありKai本人の操作が必要） |

### ここで見つかった問題

1. **`parseJwt()` は署名を検証していない。** base64デコードしてJSONにするだけ。
2. **nonce を生成しているが、戻ってきた時に検証していない。** リプレイを防げない。
3. トークンはメールアドレスを取り出したら**捨てられ、サーバーへは送られない**。

つまりクライアント側ですら本人確認になっていない。
ただし**サーバーが検証すれば全部解決する**問題でもある（1と2はサーバー側検証で無意味になる）。

### デプロイ棚卸し ─ 完了

9個 → 3個。旧6個は認証なしで応答することを実測後に削除。詳細は [DEPLOYMENTS.md](DEPLOYMENTS.md)。

---

## tokeninfo（A案）vs 専用検証サービス（B案）

| 比較項目 | A案: tokeninfo | B案: Cloud Run 等 |
|---|---|---|
| セキュリティ | Googleが検証。ただし公式は「開発・デバッグ用」 | 公式ライブラリで本番想定 |
| 実装日数 | 0.5日 | 2〜3日（インフラ・デプロイ・監視込み） |
| 月額費用 | 0円 | Cloud Run で概ね0〜数百円（無料枠内の見込み） |
| 運用負担 | なし | サービス1つ増える。デプロイ・鍵・監視の管理 |
| GASとの接続 | UrlFetchApp で1回叩くだけ | UrlFetchApp＋共有鍵で署名検証 |
| 障害時の影響 | Google側障害＝ログイン不可（既存も同じ） | 自前サービス障害＝ログイン不可。**障害点が増える** |
| 法人提供へそのまま使えるか | **使えない。移行が必要** | そのまま使える |

**推奨: A案で即座に穴を塞ぎ、法人提供の開始前にB案へ移行する。**

理由は、現在**無認証で個人情報が露出している**ことが最大のリスクであり、
B案の2〜3日を待つ間もその状態が続くため。A案は0.5日で塞げる。

**移行期限**: 法人向け（JIROKU for Business）の最初の商談前、
または2026年内のいずれか早い方。`AUTH_VERIFIER_MODE` を持たせ、
現在どちらで動いているかをコードと管理画面から分かるようにする。

**A案の必須条件**:
- `aud` が自分のクライアントIDと一致すること
- `iss` が `accounts.google.com` または `https://accounts.google.com`
- `exp` が未来
- `email_verified` が true
- **tokeninfo が失敗したらログインを拒否する。メール文字列へフォールバックしない**

---

## 本人IDは google_sub

メールアドレスは変更されうるので主キーにしない。Users に以下を追加する。

| 列 | 内容 |
|---|---|
| `user_id` | 内部の不変ID |
| `google_sub` | Googleの `sub`。**これが本人の実体** |
| `email` | 表示・連絡用。変わりうる |
| `token_version` | 上げると全端末を一括失効 |
| `role` | USER / COACH / MANAGER / ORG_ADMIN / JIROKU_ADMIN |
| `organization_id` | 法人向け |
| `auth_linked_at` | 初回に sub を紐づけた日時 |

### 既存24人の紐づけ（初回ログイン時に1回だけ）

```
1. IDトークンを検証する
2. 検証済みの sub と email を取り出す（クライアントが送った値は使わない）
3. その sub が既にどれかのユーザーに登録されている
   → そのユーザーとして扱う（メールが変わっていても同一人物）
4. sub が未登録
   → 検証済み email と一致する既存ユーザーを探す
     ・見つかった、かつその行に sub が未設定 → sub を書き込む（1回だけ）
     ・見つかった、かつ別の sub が設定済み   → 拒否（別人の可能性）
     ・見つからない                          → 新規登録の導線へ
```

---

## セッション設計

### 保存先の比較

| 保存先 | 速度 | 複数端末 | 一覧・失効の管理 | 判定 |
|---|---|---|---|---|
| ScriptProperties | 速い | 可 | キーが増え続け、一覧性が悪い | 単独では不可 |
| Sessions シート | 遅い（読取1回） | 可 | **管理しやすい。監査にも使える** | 台帳として採用 |
| CacheService | **非常に速い** | 可 | 揮発する（最大6時間） | 前段のキャッシュとして採用 |

**推奨: Sessions シートを台帳とし、CacheService を前段に置く二段構え。**

毎リクエストでシートを読むと現状7秒台のAPIがさらに遅くなる。
検証結果（`token_hash → user_id / role / expires_at`）をCacheServiceに
短時間だけ載せ、失効やロール変更のときはキャッシュを消す。
書き込みは `LockService` で保護する（既存の `p1Upsert` と同じ方式）。

### スキーマ

```
Sessions:
  session_token_hash   sha256。平文は保存しない
  user_id
  google_sub
  role
  organization_id
  expires_at
  created_at
  last_seen_at
  revoked_at
  token_version
```

要件（すべて満たす）:
- 平文トークンはシート・ログ・ScriptProperties のどこにも保存しない
- 32バイトの暗号学的乱数（`Utilities.getUuid()` は乱数として弱いので使わない）
- ハッシュで突き合わせる
- ログアウトで `revoked_at` を立てる
- `token_version` を上げれば全端末を一括失効
- ロール変更時に既存セッションを無効化
- 期限切れは fail-closed
- **トークンが無い時に studentEmail 認証へ戻さない**

---

## 入口を fail-closed にする

「保護するAPIを列挙する」のではなく、**原則すべて認証必須にして公開だけ例外**にする。
そうしないと、新しいAPIを足したときに認証を忘れて自動的に公開される。

```javascript
const PUBLIC_ACTIONS = { login: 1, healthCheck: 1 };  // ここに書いたものだけ素通り

function doGet(e) {
  const action = e.parameter.action;
  if (!PUBLIC_ACTIONS[action]) {
    const auth = verifySession(e.parameter.token);
    if (!auth.ok) return jsonResponse({ ok: false, error: "AUTH_REQUIRED" });
    e.__auth = auth;   // 以降、本人はここからしか取らない
  }
  ...
}
```

**`e.parameter.studentEmail` は読まない。** `e.__auth.email` を使う。
既存のアクションは `studentEmail` を引数で受け取る形になっているので、
入口で差し替えれば143個を個別に直す必要はない。

---

## ロールと対象範囲

ロールだけで通さず、**誰のデータかまで確認する**。

| ロール | 見られる範囲 |
|---|---|
| USER | 自分だけ |
| COACH | CoachingNotes や担当関係で紐づく利用者の COACH_REVIEW のみ |
| MANAGER | 許可された MANAGER_REPORT のみ |
| ORG_ADMIN | 自組織の ORG_AGGREGATE と組織設定のみ |
| JIROKU_ADMIN | 運営に必要な範囲 |

各APIに「必要ロール／対象ユーザー／organization_id／担当関係／公開レベル」を定義する。
**MANAGER と ORG_ADMIN に PRIVATE の生ログは返さない。**

---

## 段階移行の日程

観測期間中も偽装可能な状態が続くという指摘は妥当。
**高リスクAPIは観測を待たずに即日必須化する。**

| 段階 | 内容 | 目安 |
|---|---|---|
| Auth CP1 | IDトークン検証・セッション基盤・共通検証関数 | 1日目 |
| Auth CP2 | **admin/coach 系を認証必須**（一括送信・削除・個人情報一覧） | 1日目中に即日 |
| Auth CP3 | 本人の書き込みAPI（saveLog / saveGoal 等）を必須化 | 2日目 |
| Auth CP4 | 本人データの読み取りAPI（getLogs 等）を必須化 | 3日目 |
| Auth CP5 | 互換モード終了・旧経路ゼロの確認 | **4日目に打ち切り** |

**未認証互換モードの期限: 開始から4日。** 無期限には残さない。
互換モードは CP3・CP4 の対象APIにのみ適用し、CP2 の高リスクAPIには最初から適用しない。
未認証の古いクライアントには `AUTH_REQUIRED` を返し、アプリ側でログイン画面へ誘導する。

各Checkpoint完了後に停止し、Kaiの確認を受けてから次へ進む。

---

## 受け入れテスト

`gas/smoke_test.sh` に追加する（すべて自動化する）。

- [ ] トークンなしで個人情報を取得できない
- [ ] 他人の studentEmail を付けても本人は変わらない
- [ ] 他人の goal_id / task_id / log_id を操作できない
- [ ] USER が coach/admin API を実行できない
- [ ] COACH が担当外の利用者を取得できない
- [ ] MANAGER が PRIVATE ログを取得できない
- [ ] 期限切れ・失効済み・改ざんトークンを拒否する
- [ ] email だけを送っても認証されない
- [ ] body で role を admin にしても権限が上がらない
- [ ] 旧デプロイURLから回避できない（**削除済みだが再発防止として毎回確認**）
- [ ] セッショントークンがログに出ない
- [ ] 一括送信・削除が監査ログに残る

---

## 見積もり

| 作業 | 目安 |
|---|---|
| Auth CP1 セッション基盤 | 1日 |
| Auth CP2 高リスクAPI保護 | 0.5日 |
| Auth CP3 書き込みAPI | 0.5日 |
| Auth CP4 読み取りAPI | 0.5日 |
| Auth CP5 互換モード終了・確認 | 0.5日 |
| 受け入れテストの自動化 | 0.5日 |
| **合計** | **約3.5日**（＋各段階の確認待ち） |

### リスク

| リスク | 対策 |
|---|---|
| 全員ログイン不可 | テスト用デプロイで先に検証。revert手順は整備済み |
| セッションシートで遅くなる | CacheService を前段に置く。CP1完了時に速度を計測して判断 |
| LINE内ブラウザでリダイレクトが戻らない | 現在も同じ方式で動作しているため同条件。CP1で実機確認する |
| 24人が同時にログアウトされる | 事前にLINEで告知してから CP5 を実施 |

---

## Checkpoint 3 の再開手順（認証完了後）

1. 集計仕様を修正する
   - `COUNT` / `DURATION` / `DAYS` / `BOOLEAN` へ命名統一
   - COUNT・DURATION は**正の quantity を必須**とし、空欄・0は加算しない
   - DURATION は ①タイマー実測の actual_minutes ②入力された quantity
     ③time_block を**候補値として初期表示**し、本人が確認して保存した時だけ加算
   - **紐づけただけでは自動加算しない**
2. フロント: 記録画面に「この目標に使った時間 [ 60 ] 分」を追加
3. 週間進捗の表示
4. 回帰テスト23項目

---

## GCP OAuth 設定の確認結果（2026-08-01・Kaiが実機で確認）

| 項目 | 実際の登録値 | 判定 |
|---|---|---|
| OAuth 2.0 クライアント | **1件のみ**（名前 JIROKU / 2026-07-01 作成） | ✅ |
| APIキー | **0件** | ✅ |
| サービスアカウント | **0件** | ✅ |
| 承認済みの JavaScript 生成元 | `https://kaisunagawa.github.io` の1件のみ | ✅ |
| 承認済みのリダイレクト URI | `https://kaisunagawa.github.io/edventure-app/` の1件のみ | ✅ |
| クライアントシークレット | 存在するが**このアプリでは使っていない**（implicit flow のため）。<br>コード・git履歴のどちらにも含まれていないことを確認済み | ✅ |

**不審な登録はゼロ。** 想定していた「他サイトからログインを偽装される」経路は存在しない。
これにより、Auth CP1 の本番切り替えに残っていた最後の条件が解消した。

※クライアントシークレットは現在の方式では不要。将来サーバーサイドのOAuthを
使う場合に備えて残っているが、使うときは必ずサーバー側（ScriptProperties）に置くこと。
