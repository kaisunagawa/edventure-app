# 認証導入計画（P0）

作成: 2026-07-31 / 状態: **設計のみ。実装は未着手（Kaiの承認待ち）**

## 1. 何が起きているか

このWeb appは**リクエストに書かれたメールアドレスをそのまま信用している**。
`doGet` は `e.parameter.studentEmail`、`doPost` は `body.studentEmail` を読むだけで、
トークン検証も `Session.getActiveUser()` も無い。

実測（2026-07-31）: 認証情報を持たない curl から `coachEmail` に管理者のアドレスを
書いただけで、**在籍生徒21名の氏名とメールアドレスを取得できた**。

### 公開状況

| 区分 | 件数 |
|---|---:|
| doGet/doPost で公開しているアクション | **143** |
| うち鍵（P1_ADMIN_SECRET）で保護済み | **5** |
| coach* アクション | 28 |
| admin* アクション | 25 |

`verifyAdmin()` / `verifyCoach()` は存在するが、**引数がクライアント由来の文字列**なので
認可として機能していない。

### 最も危険なもの（被害の大きさ順）

| アクション | 何ができてしまうか |
|---|---|
| `adminBroadcastLine` | **全利用者へLINEを一斉送信** |
| `adminSendStudentCampaign` | **全利用者へ一斉メッセージ** |
| `coachSendStudentMessage` | コーチを装って個別にメッセージ送信 |
| `coachGetStudentDetail` | 特定個人の氏名・記録・コーチング記録を全取得 |
| `coachGetStudents` | 全生徒の一覧と連絡先（**実測で取得成功**） |
| `adminTagCohortByEmails` 他 | 利用者データの一括書き換え |
| `getLogs` / `getReportList` / `getHomeData` | 任意個人の行動記録・日記・AI所見 |
| `coachUploadFile` / `coachDeleteFile` | 契約書ファイルの追加・削除 |

**評価**: 読み取りだけでなく**送信と破壊**ができる。人数ではなくデータの機密性で
判断すべきというレビュー指摘は妥当。

### Checkpoint 2/3 で追加したAPIの位置づけ

`saveGoal` / `saveWeeklyGoal` / `archiveGoalItem` / `getGoalTree` / `migrateLocalTasks` は
`goals_v1` フラグ必須で、現在の対象は**Kai 1人だけ**。

これらを止めても**リスクはほぼ減らない**。攻撃者がKaiを騙れる時点で、
上の表にある既存APIの方が桁違いに危険だから。
一方でKaiは既に実データの3か月目標を入れて使い始めている。
→ **止める実益がなく、使用中の機能を壊すだけなので、停止は推奨しない。**
本当に減らすべきは既存の高リスクAPIの露出。

### アクセスログの確認について

**確認できていない。** `clasp logs` はGCPプロジェクト未設定で使用不可。
Apps Scriptエディタの「実行数」画面からは手動で確認できるが、
匿名Webアプリの呼び出しは呼び出し元を識別できず、保持期間も限られる。
**「不正アクセスが無かった」とは言えない**ことを明記しておく。

---

## 2. 認証の現実解 ─ すでに8割できている

**重要な発見**: このアプリは**すでに Google ログインを実装している**。

`index.html` は Google Identity Services で `response_type=id_token` を要求し、
署名付きの `id_token`（JWT）を受け取っている。ところが `parseJwt()` で
**クライアント側で中身を読み、メールアドレスの文字列だけ取り出して捨てている**。
サーバーはこのトークンを一度も見ていない。

```
現在: [Google] --id_token--> [ブラウザ] --email文字列だけ--> [GAS]  ← 誰でも詐称できる
あるべき: [Google] --id_token--> [ブラウザ] --id_token--> [GAS] --検証--> email確定
```

つまり**本人確認の仕組みは既にあり、それをサーバーまで運んでいないだけ**。
ゼロから認証基盤を作る話ではない。

### 方式の比較

| 方式 | 実装量 | LINE内ブラウザ | 評価 |
|---|---|---|---|
| **Google ID token 検証 ＋ 自前セッション** | **小** | ○（現在も動作中） | **推奨** |
| メールのワンタイムコード | 中 | ○ | 既存Googleログインと二重になる |
| マジックリンク | 中 | △ 別ブラウザで開くと切れる | 不採用 |
| LINE連携による本人確認 | 中 | ○ | LINE未連携者が使えない |
| 署名付きセッションのみ（本人確認なし） | 小 | ○ | 最初の本人確認が無いので不可 |

**推奨: Google ID token を検証 → サーバー発行の短命セッショントークンへ交換。**
理由は ①既存のログイン導線をそのまま使える ②利用者の操作が増えない
③LINE内ブラウザで既に動作実績がある ④追加の外部サービスが不要。

### 検証のしかた（GASでの現実的な実装）

GASにはRSA署名の**検証**関数が無いため、JWTをローカル検証するのは面倒。
`https://oauth2.googleapis.com/tokeninfo?id_token=...` へ問い合わせる方式にする。
毎回叩くと遅いので、**初回だけ検証してセッショントークンを発行**する。

```
POST action=login  { idToken }
  → tokeninfo で検証（aud が自分の client_id か / exp / email_verified）
  → セッショントークン（乱数32バイト）を発行して返す
  → サーバーには sha256 ハッシュだけ保存（平文は保存もログ出力もしない）

以降の全リクエスト  { token }
  → ハッシュで引いて email / role / 有効期限 / token_version を確定
  → studentEmail・coachEmail のクライアント入力は一切信用しない
```

### Kai限定の暫定方式（Phase 1検証中）

正式版まで待たずに、Kaiのアカウントだけ先に閉じることができる。
上の仕組みの「ログイン」部分を省き、**サーバーで1本だけトークンを発行**して
Kaiの端末に一度だけ設定する。

- 発行はエディタ実行の関数で行い、**index.html には絶対に埋め込まない**
- ScriptProperties にはハッシュのみ保存
- 有効期限あり・`token_version` を上げれば即失効
- 平文はログに出さない

ただし**これはKai個人しか守らない**。生徒21名の情報が露出している状況は変わらないので、
暫定方式に時間をかけるより正式版を早く出す方が良いと考える。

---

## 3. ロールとスコープ

```
USER         自分のデータのみ
COACH        担当している利用者の COACH_REVIEW 範囲のみ
MANAGER      許可された MANAGER_REPORT のみ
ORG_ADMIN    組織設定と ORG_AGGREGATE
JIROKU_ADMIN 運営に必要な管理権限
```

公開レベル（[PHASE1_BASELINE.md](PHASE1_BASELINE.md) と共通）:

```
PRIVATE         本人のみ。生の記録・感情・健康・自由記述・AI会話・失敗内容
COACH_REVIEW    運営／担当コーチ
MANAGER_REPORT  上司。抽出された業務レポートのみ
ORG_AGGREGATE   組織管理者。個人を特定しない集計値
```

**MANAGER と ORG_ADMIN は PRIVATE の生ログを取得できない。**
ロールはトークンから確定し、クライアントが送る `role` は根拠にしない。

トークンから確定する項目: `user_id` / `student_email` / `role` /
`organization_id` / `coach権限` / `有効期限` / `token_version`

---

## 4. 既存24人の移行

利用者の操作は**「もう一度ログインするだけ」**。
ログイン方法自体は今までと同じGoogleログインなので、新しく覚えることは無い。

1. サーバー側にトークン検証を入れる（この時点ではまだ強制しない）
2. フロントを更新し、ログイン時にトークンを取得・保存して全リクエストに付ける
3. 数日、**どのリクエストがトークン付きか**を記録して到達率を見る
4. 100%に達したら、トークン必須へ切り替え
5. 古いセッションは無効化され、次回起動時に自動でログイン画面へ

**旧デプロイの無効化**: 認証導入後、過去のデプロイURLが残っていると
そこから認証なしで叩けてしまう。`clasp deployments` で一覧を出し、
本番・テスト以外は `clasp undeploy` で削除する。テスト用デプロイも
同じ認証を通す（素通しの入口を残さない）。

---

## 5. 見積もりとリスク

| 作業 | 目安 |
|---|---|
| サーバー: トークン発行・検証・doGet/doPost の入口改修 | 0.5日 |
| サーバー: coach/admin 系の認可をロール判定へ置き換え | 0.5日 |
| フロント: ログイン時のトークン取得と全リクエストへの付与 | 0.5日 |
| 到達率の観測と切り替え | 0.5日（実時間は数日） |
| 合計 | **約2日＋観測期間** |

入口が `doGet`/`doPost` の2箇所に集約されており、ほとんどのアクションが
`studentEmail` を引数で受け取る形になっているため、**143個を個別に直す必要はない**。
入口で確定した本人を流し込めば大半が片付く。

### リスク

| リスク | 対策 |
|---|---|
| 全員がログインできなくなる | テスト用デプロイで先に検証。段階的に切り替え。revert手順は整備済み |
| tokeninfo の呼び出しで遅くなる | ログイン時のみ。以降は自前トークンで、外部通信なし |
| LINE内ブラウザでトークンを保持できない | 現在も localStorage でログイン状態を保持できているので同条件 |
| 切り替え中の隙間 | 高リスクAPI（送信・一括操作）から先に必須化する |

---

## 6. 自動バックアップ（設計のみ）

```
毎週1回（日曜 03:00）トリガー
  SpreadsheetApp.copy() で複製          ← Drive権限の承認が不要
  シート数と主要件数を元ファイルと突き合わせ
  getEditors/getViewers で外部公開が無いことを確認
  8週間より古いバックアップを削除
  結果をLINEで管理者へ通知（成功・失敗とも）
```

**安全装置（必須）**:
- 削除対象は名前が `JIROKU_backup_` で始まるものだけ
- **本番のスプレッドシートIDと一致するものは、いかなる場合も削除しない**（ID照合を先に行う）
- 1回の実行で削除できるのは最大3件（暴走時の被害を限定）
- 削除前に必ず件数を記録し、通知に含める

**復元手順**: バックアップのコピーを作る → 新しいコピーのIDを `SPREADSHEET_ID` に設定
→ `bash gas/push_gas.sh` → スモークテストで確認。
元ファイルを直接編集して戻さない（失敗したときに戻る場所が無くなるため）。

---

## 7. Checkpoint 3 の集計仕様（再開前に直す）

レビュー指摘を受けた修正点。**まだ直していない。**

### metric_type の命名を統一する

現在 `count` / `minutes` / `boolean`（小文字）。
→ **`COUNT` / `DURATION` / `DAYS` / `BOOLEAN`** に統一する。
`minutes` と `duration` の混在をやめる。
※既存データは検証用を含め全て削除済みなので、**移行は不要**。

### COUNT

現在は quantity 未入力を 1 として加算している。
→ **正の数の quantity がある時だけ加算する。**

*補足*: これは正しいが、記録しても進捗が 0 のままだと「壊れている」と感じられる。
UI側で、COUNT の週間目標に紐づけた時は**数量入力を必須にする**必要がある。

### DURATION

現在は time_block 全体を自動で実績にしている。
→ 優先順位を **①ユーザーが入力した quantity ②完了タスクの actual_minutes
③どちらも無ければ加算しない** に変更する。

*補足*: ただし「その記録をこの週間目標に紐づける」という操作自体が、
**その時間枠をその目標に使ったという本人の明示**にあたる。
紐づけがある場合に time_block から算出するのは、レビューが認めた例外に該当すると考える。
毎回分数を手入力させると記録の手間が増え、記録すること自体をやめてしまう。
→ **紐づけ済みの場合のみ time_block を採用**する案で合意を取りたい。

### BOOLEAN

本人が週間目標を明示的に完了にした時だけ 1。**現状のままでよい。**

### DAYS

有効な `primary_weekly_goal_id` を持つログの日付を重複なしで数える。**未実装。**

---

## 8. 次にやること

1. この計画へのKaiの承認
2. 高リスクAPI（送信系・一括操作系）を先に封じる
3. Google ID token 検証とセッション発行の実装
4. 段階的な切り替え
5. 自動バックアップ
6. 集計仕様を直して Checkpoint 3 を再開
