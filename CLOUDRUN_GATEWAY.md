# セキュリティゲートウェイ設計（Cloud Run）

作成: 2026-08-01 / **状態: 設計のみ。サービス作成・課金設定・Webhook URL変更は未実施**

---

## 0. なぜ必要か

**GASはHTTPリクエストヘッダーを取得できない。** 実測で確認済み — `doPost(e)` の `e` が
持つのは `contentLength` / `contextPath` / `parameters` / `queryString` / `parameter` /
`postData` だけで、ヘッダーは含まれない。

LINEの署名は `x-line-signature` ヘッダーで届くため、**GAS内では原理的に検証できない**。
現在は「未署名の書き込み入口」が本番に存在している状態で、暫定のワンタイム連携トークンで
被害範囲を「有効なコードによる初回連携のみ」に絞っているに過ぎない。

同時に、Google IDトークンの検証も `tokeninfo` エンドポイントへの問い合わせに依存している。
これは1回のログインにつき外部への往復が1回発生し、Googleの提供が止まれば
ログインも止まる。GASにはRSA署名の検証手段が無いため、これもGAS内では解決できない。

**どちらも「GASでは無理」が理由なので、受け口をひとつ外に置いて両方まとめて解く。**

---

## 1. 推奨アーキテクチャ

```
[LINE Platform]
     │ POST + x-line-signature
     ▼
┌─────────────────────────────────────┐
│ Cloud Run: jiroku-gateway           │
│  ・未認証呼び出しを許可（LINE用）    │
│  ・最小インスタンス 0（コスト0円待機）│
│                                     │
│  POST /line/webhook                 │
│    raw body を一切変更せず取得       │
│    HMAC-SHA256 で署名検証            │
│    webhookEventId で冪等判定         │
│    → 内部署名を付けてGASへ転送        │
│                                     │
│  POST /auth/verify                  │
│    GASからの内部署名を検証            │
│    google-auth-library でIDトークン検証│
│    → 確定した sub/email だけを返す     │
└─────────────────────────────────────┘
     │ 内部署名(HMAC)          ▲ 内部署名(HMAC)
     ▼                        │
┌─────────────────────────────────────┐
│ GAS Web App（既存）                  │
│  doPost: gatewayイベントを処理        │
│  authLogin: /auth/verify を呼ぶ       │
└─────────────────────────────────────┘
     │
     ▼  Google Sheets（既存・変更なし）
```

**設計方針: ゲートウェイは状態を持たない薄い層にする。**
業務ロジックは全てGASに残す。理由は ①移行の差分を小さくする
②ゲートウェイが落ちてもGAS側の日次処理・レポート生成は動き続ける
③戻すときにWebhook URLを1つ書き換えるだけで済む。

唯一の例外が冪等性の記録で、これはFirestoreではなく
**GAS側の既存スプレッドシートに `WebhookEvents` シートを作って持たせる**
（後述。ゲートウェイを本当に無状態にできる）。

### 却下した案

| 案 | 却下理由 |
|---|---|
| Cloud Functions (第1世代) | Cloud Runへ統合が進んでおり、新規は非推奨 |
| Apps Script の doPost で署名検証 | **ヘッダーが取得できないので不可能**（本設計の出発点） |
| Firestore で冪等管理 | サービスが1つ増える。件数が少なく、既存シートで足りる |
| API Gateway を前段に置く | 月額が乗るだけで、この規模では得るものが無い |
| ゲートウェイへ業務ロジックを移す | 移行が大きくなり、障害時にJIROKU全体が止まる |

---

## 2. 使用言語とランタイム

**Node.js 22 (LTS) / TypeScript / Cloud Run 第2世代**

| 項目 | 選定 | 理由 |
|---|---|---|
| 言語 | TypeScript | `google-auth-library` と `@line/bot-sdk` の公式サポートが最も厚い |
| ランタイム | Node.js 22 LTS | 2027年4月までメンテナンス対象 |
| フレームワーク | Hono | Expressより軽く、**raw bodyの取得が素直**。ここは事故が起きやすい要所 |
| コンテナ | `node:22-slim` ベース、distroless で本番用 | 攻撃面を減らす |
| ビルド | Cloud Build（GitHub push で自動） | 手動デプロイの取り違えを防ぐ |

**raw body の扱いが最重要。** JSONパーサを先に通すとバイト列が変わり、
署名が一致しなくなる。Honoでは `await c.req.arrayBuffer()` で生バイトを取り、
**検証が通ってから初めて** `JSON.parse` する。順序を逆にすると
「検証前にパースした結果で処理してしまう」という典型的な事故になる。

---

## 3. エンドポイント

| メソッド | パス | 呼び出し元 | 認証 |
|---|---|---|---|
| POST | `/line/webhook` | LINE Platform | `x-line-signature`（HMAC-SHA256） |
| POST | `/auth/verify` | GAS | 内部署名（HMAC-SHA256） |
| GET | `/healthz` | Cloud Run / 監視 | なし（状態を返すだけ。秘密情報を含めない） |

**それ以外のパスは404を返し、ログにも残さない**（探索行為でログを膨らませないため）。

### 3-1. `POST /line/webhook`

```
1. raw body を Buffer で取得（一切変更しない）
2. x-line-signature ヘッダーを取得
3. HMAC-SHA256(LINE_CHANNEL_SECRET, rawBody) を base64 化し、
   timingSafeEqual で比較
   ─ ヘッダー無し / 不一致 → 401、body は一切パースしない、処理もしない
4. ここで初めて JSON.parse
5. events[] の各要素について webhookEventId で冪等判定（§7）
6. 内部署名を付けてGASへ転送
7. LINEへは常に 200 を返す（§8）
```

**「検証成功後だけイベントを処理」を構造で担保する。**
署名検証を通っていないコードパスから `JSON.parse` を呼べないよう、
パース処理を検証済みバイト列を引数に取る関数の中だけに置く。

### 3-2. `POST /auth/verify`

```
1. 内部署名（X-Jiroku-Signature / X-Jiroku-Timestamp / X-Jiroku-Nonce）を検証
   ─ timestamp が ±120秒 を外れたら拒否
   ─ nonce が既出なら拒否（インスタンス内キャッシュ＋GAS側の最終防衛）
   ─ payload hash が署名対象に含まれる（body改ざんを検知）
2. google-auth-library の OAuth2Client.verifyIdToken() で検証
   ─ audience = JIROKUのclient_id
   ─ iss は accounts.google.com / https://accounts.google.com のみ
   ─ exp / iat を確認
   ─ email_verified === true
   ─ sub が存在する
   ─ nonce がGASの発行したチャレンジと一致
3. 成功時は { sub, email, email_verified, nonce } だけを返す
   ─ IDトークン本体も、その他のクレームも返さない
4. 失敗時は理由コードのみ返し、fail-closed
   ─ ★email文字列へのフォールバックは行わない★
   ─ 検証できなければ「本人不明」であって「emailの人」ではない
```

公式ライブラリはGoogleの公開鍵を取得してローカルでRSA署名を検証し、鍵をキャッシュする。
`tokeninfo` と違い**ログインのたびにGoogleへ往復しない**ので、速くなり、
Googleの一時的な不調にも強くなる。

---

## 4. 認証・署名方式

### 4-1. LINE → ゲートウェイ

LINE公式仕様どおり。`HMAC-SHA256(channelSecret, rawBody)` を base64 化し、
`x-line-signature` と**定数時間比較**する。長さが違う場合も含めて
比較関数の中で吸収し、早期returnで長さを漏らさない。

### 4-2. ゲートウェイ ⇄ GAS（内部署名）

**LINEチャネルシークレットとは別の鍵を使う。**（ChatGPTの指示どおり。
1本の鍵を使い回すと、片方が漏れたときに両方の入口が開く）

```
署名対象（この順で連結、区切りは \n）:
  METHOD \n PATH \n TIMESTAMP \n NONCE \n SHA256(rawBody)

ヘッダー:
  X-Jiroku-Timestamp : UNIX秒
  X-Jiroku-Nonce     : 128bit乱数（base64url）
  X-Jiroku-Signature : base64url(HMAC-SHA256(鍵, 署名対象))
  X-Jiroku-Key-Id    : 鍵の世代（無停止ローテーション用）
```

- **timestamp**: ±120秒。ゲートウェイもGASもUTCで扱う
- **nonce**: 一度きり。ゲートウェイ側はインスタンス内メモリ、GAS側は CacheService（300秒）
- **payload hash**: bodyを署名対象に含めるので、途中で1バイト変えても検知できる
- **Key-Id**: 新旧2世代を同時に受け付ける期間を作り、鍵交換で停止しないようにする

**GAS側は受信ヘッダーを読めない** ので、GAS宛の転送では
**ヘッダーではなくJSON body の `_sig` フィールド**に同じ内容を入れる。
（この非対称は設計上の妥協点。ゲートウェイ→GAS方向だけbody内署名になる）

### 4-3. Cloud Run 自体のアクセス制御

| エンドポイント | 設定 |
|---|---|
| `/line/webhook` | 未認証呼び出しを許可（LINEはIAMトークンを送れないため）。**防御は署名検証のみ**なので、ここが最重要 |
| `/auth/verify` | 同上（GASからIAMトークンを付けるのは可能だが、GASのOAuthスコープが増えるため内部署名で統一） |

未認証公開になるため、**Cloud Armor でレート制限をかける**（§10）。

---

## 5. Secret Manager の構成

**用途ごとに鍵を分ける。1本を共用しない。**

| シークレット名 | 中身 | 使う場所 | ローテーション |
|---|---|---|---|
| `line-channel-secret` | LINEチャネルシークレット | Webhook署名検証 | LINEコンソール側で再発行時 |
| `line-channel-token` | LINEチャネルアクセストークン | （当面GAS側に残す） | 同上 |
| `gw-gas-hmac-v1` | ゲートウェイ⇄GAS の内部署名鍵 | 両方向 | 90日ごと。Key-Idで無停止 |
| `google-oauth-client-id` | JIROKUのclient_id | IDトークンのaud確認 | 変更時のみ |

- **コードにもGitHubにも入れない。**`.env` も置かない
- Cloud Run へは**環境変数ではなくボリュームマウント**で渡す
  （環境変数はコンソールの画面やエラー出力に載ることがある）
- サービスアカウントは専用に1つ作り、`roles/secretmanager.secretAccessor` のみ
- **GAS側の `P1_ADMIN_SECRET`（管理CLI用）とは完全に別物**にする。
  現在は鍵1本で運用しているが、この機会に管理CLI用と内部通信用を分離する

### 鍵のローテーション手順（無停止）

```
1. gw-gas-hmac-v2 を作成
2. GAS側を「v1とv2の両方を受け付ける」状態にしてデプロイ
3. ゲートウェイを v2 で署名するよう更新
4. 監査ログで v1 の使用が0件になるのを確認
5. GAS側から v1 を外す → v1 を無効化
```

---

## 6. GASとの接続方法

### 6-1. ゲートウェイ → GAS（LINEイベント転送）

```
POST https://script.google.com/macros/s/<PROD_DEPLOYMENT_ID>/exec
Content-Type: text/plain;charset=utf-8

{
  "action": "gatewayLineEvent",
  "_sig": { "ts": ..., "nonce": "...", "keyId": "v1", "sig": "..." },
  "event": { ...LINEのイベント1件... }
}
```

- **1リクエスト1イベント**にする。GASの実行時間上限（6分）に対して、
  1回のWebhookに複数イベントが入ると片方の失敗がもう片方を巻き込むため
- GAS側に `case "gatewayLineEvent"` を追加し、**内部署名を検証してから**処理する
- 既存の `body.events` を処理する経路は、移行完了後に削除する
  （それまでは両方受けるが、**新しい経路が有効な間は旧経路を無効化**して二重処理を防ぐ）

### 6-2. GAS → ゲートウェイ（IDトークン検証）

`verifyIdToken()` の中身を差し替える。呼び出し側は変更不要。

```javascript
// AUTH_VERIFIER_MODE = "gateway" のとき /auth/verify を使う。
// "tokeninfo" なら従来どおり。プロパティ1つで切り戻せる。
```

**すでに `AUTH_VERIFIER_MODE` というプロパティが存在する**（現在 `tokeninfo-2026-08`）。
移行はこの値の切り替えだけで済むよう作ってある。

---

## 7. LINE再送と冪等性

LINEは200が返らないと**最大3回まで再送**する。ネットワークの都合で、
処理は成功したのに応答が届かない場合もある。同じイベントを2回処理すると、
連携コードが二重に消費される・返信が2通届く、といった実害が出る。

```
判定キー: webhookEventId（LINEが各イベントに付ける一意ID）

┌ ゲートウェイ ─────────────────────┐
│ インスタンス内 LRU（直近1000件・TTL 10分）  │ ← 高速な1次防御
│  ヒット → GASへ転送せず 200 を返す          │
└──────────────────────────────┘
              │ ミス
              ▼
┌ GAS ────────────────────────────┐
│ WebhookEvents シート（新設）                 │
│  列: webhook_event_id / received_at /       │
│      event_type / result / processed_at     │
│  LockService で排他してから存在確認・追記     │
│  既出 → 何もせず ok を返す                   │
│  7日より古い行は日次で削除                    │
└──────────────────────────────┘
```

**なぜ2段なのか**: Cloud Runは複数インスタンスに分かれるため、
メモリだけでは別インスタンスへの再送を取りこぼす。逆にGASだけだと
毎回シートアクセスが発生して遅い。**最終的な正しさはGAS側が担保**し、
メモリは負荷を減らすだけの役割に留める。

**LINEへの応答は常に 200。** GASが失敗しても200を返す。
理由は、LINEに再送させても同じ理由で失敗する可能性が高く、
再送で復旧するより**失敗を記録して後から手当てする方が確実**なため。
失敗はゲートウェイのログとGASの `WebhookEvents.result` に残す。

---

## 8. 障害時の挙動

| 障害 | 挙動 | 利用者への影響 |
|---|---|---|
| ゲートウェイが落ちる | LINEへ200が返らず最大3回再送 → 最終的にイベント消失 | **LINEからの連携コード送信ができない。** 記録・レポート・リマインダーは無事（GASのトリガーは独立） |
| GASが落ちる／遅い | ゲートウェイは10秒でタイムアウトし、`result=GAS_TIMEOUT` を記録して200 | 同上 |
| Secret Manager が読めない | 起動時に失敗させ、リクエストを一切受けない（**fail-closed**） | 同上。**署名検証なしで通すことは絶対にしない** |
| `/auth/verify` が落ちる | GASの `verifyIdToken` が失敗 → **ログイン不可** | ★影響大。`AUTH_VERIFIER_MODE=tokeninfo` へ戻せば即復旧 |
| Googleの公開鍵取得に失敗 | ライブラリのキャッシュで数時間は耐える。切れたら fail-closed | 新規ログインのみ不可。既存セッションは有効 |
| 内部署名の鍵が不一致 | 全転送が拒否される | LINE連携のみ停止。Key-Id 2世代受付で通常は起きない |

**設計上の判断**: `/auth/verify` の障害はログイン全体を止めるので、
**tokeninfo方式をコードから消さず、切り替えプロパティで残す**。
「新方式が安定したら旧方式を消す」を守りつつ、消すのは最低1か月運用してから。

---

## 9. ロールバック方法

| 何を戻すか | 手順 | 所要 | 影響 |
|---|---|---|---|
| IDトークン検証 | `AUTH_VERIFIER_MODE=tokeninfo` に戻す | **数秒**・デプロイ不要 | なし |
| LINE Webhook | LINE Developers で Webhook URL をGASのURLへ戻す | **1分**・GAS側は旧経路を残しておく | 戻した瞬間から署名検証が無くなる（元の状態） |
| ゲートウェイ全体 | Cloud Run のリビジョンを1つ前へ戻す | 1分 | なし |
| 完全撤退 | 上2つを戻し、Cloud Runサービスを削除 | 5分 | なし。GASは一切変更していない状態に戻る |

**旧経路（GASの `body.events` 処理）は移行後も最低1か月残す。**
消すのは、ゲートウェイ経由の処理が1か月無事故で回ってから。

---

## 10. ログ・監視・通知

### ログに残さないもの（ChatGPTの指示に対応）

- **メッセージ本文**（連携コードが含まれうる）
- LINE userId の**全体**（先頭8文字のハッシュのみ）
- メールアドレス（ハッシュのみ）
- 署名値・鍵・IDトークン

### 残すもの

```
{ ts, path, result, reason, eventType,
  eventIdHead: "先頭8文字", userHash: "sha256の先頭10文字",
  latencyMs, gasStatus }
```

### 監視

| 指標 | しきい値 | 通知先 |
|---|---|---|
| 署名検証の失敗率 | 5分間で10件超 | LINE（Kai宛）＋メール |
| GAS転送の失敗 | 5分間で3件超 | 同上 |
| `/auth/verify` の失敗率 | 5分間で20%超 | 同上 |
| p95レイテンシ | 3秒超が5分継続 | メールのみ |
| Cloud Run の 5xx | 1件でも | 同上 |

Cloud Monitoring のアラートポリシー → Pub/Sub → 既存の `notifyAdmin` を叩く
（LINE通知の仕組みはGASに既にある）。

**Cloud Armor**: `/line/webhook` はLINEの公開IPレンジからのみ許可し、
それ以外は1分100リクエストでレート制限。未認証公開の入口なので必須。

---

## 11. 月額費用の概算

現在の規模（利用者38名・LINE連携21名）で見積もる。

**リクエスト数の見積もり**
```
LINE Webhook  : 連携コード送信・友だち追加・その他メッセージ
                → 多く見て 1日50件 = 月1,500件
IDトークン検証 : 38名 × 週2回ログイン = 月330件
合計          : 月およそ 2,000リクエスト
```

| 項目 | 単価 | 月額 |
|---|---|---|
| Cloud Run リクエスト | 200万件まで無料 | **0円** |
| Cloud Run CPU/メモリ | 月18万vCPU秒まで無料。実使用 約200vCPU秒 | **0円** |
| Cloud Build | 1日120分まで無料。デプロイ月10回×2分 | **0円** |
| Artifact Registry | 0.5GB無料。イメージ約150MB | **0円** |
| Secret Manager | 6シークレット×0.06ドル＋アクセス1万回まで無料 | **約60円** |
| Cloud Logging | 月50GiB無料。実使用 数MB | **0円** |
| Cloud Monitoring | 基本は無料 | **0円** |
| Cloud Armor | ポリシー1つ 5ドル＋ルール1つ 1ドル | **約900円** |

```
Cloud Armor あり : 月およそ 1,000円
Cloud Armor なし : 月およそ 60円（ほぼ無料枠に収まる）
```

**判断が要る点**: Cloud Armor に月900円かけるかどうか。
無くても署名検証は効くので**セキュリティ上は必須ではない**。効くのは
「大量の不正リクエストで無料枠を食い潰される」ケースへの備え。
**最初は無しで始め、実際の攻撃トラフィックを見てから入れる**ことを推奨する。
その場合の月額は**約60円**。

利用者が500名に増えても、リクエスト数は月2.5万件程度で
**無料枠に収まったまま**。この構成は規模が10倍になっても費用がほぼ変わらない。

---

## 12. 実装工数

| 作業 | 目安 |
|---|---|
| プロジェクト初期化・Dockerfile・Cloud Build設定 | 0.5日 |
| `/line/webhook`（raw body・署名検証・冪等・転送） | 1日 |
| `/auth/verify`（内部署名・IDトークン検証） | 0.5日 |
| GAS側: `gatewayLineEvent` の受け口と内部署名の検証 | 0.5日 |
| GAS側: `WebhookEvents` シートと冪等処理 | 0.5日 |
| GAS側: `verifyIdToken` の切り替え対応 | 0.5日 |
| Secret Manager・サービスアカウント・IAM | 0.5日 |
| テスト（正常系・未認証・改ざん・期限切れ・リプレイ・再送・空応答・タイムアウト・ロールバック） | **1.5日** |
| 監視・アラート・通知の配線 | 0.5日 |
| 段階移行と観測 | 0.5日＋実時間で1週間 |
| **合計** | **約6.5日＋観測1週間** |

テストに1.5日を積んでいるのは、今回 `forceSelfEmail` の配線漏れを
コードレビューでは見つけられず、**実際に叩いて初めて発覚した**ため。
署名検証は「実装したつもりで効いていない」が最も起きやすい領域なので、
ここを削らない。

---

## 13. tokeninfo からの移行手順

```
1. /auth/verify を実装し、Cloud Runへデプロイ（まだGASからは呼ばない）
2. GAS側に「両方で検証して結果を突き合わせる」比較モードを入れる
     AUTH_VERIFIER_MODE = "compare"
     ・判定にはtokeninfoの結果を使う（挙動は変わらない）
     ・ゲートウェイの結果と食い違ったら監査ログに記録
3. 1週間運用し、食い違い0件を確認
4. AUTH_VERIFIER_MODE = "gateway" へ切り替え（プロパティのみ・デプロイ不要）
5. 1週間監視。問題があれば "tokeninfo" へ戻す（数秒）
6. 1か月無事故なら、compare/tokeninfo の経路を削除
```

**比較モードを挟むのが要点。** いきなり切り替えると、
食い違いがあったときに「ログインできない」という形で利用者に出る。
判定は従来どおりにしたまま裏で新方式を走らせれば、**誰にも影響を与えずに差分が取れる**。

---

## 14. LINE Webhook 移行手順

```
1. GAS側に case "gatewayLineEvent" を追加してデプロイ
   ・内部署名を検証してから処理
   ・この時点ではLINEからの直接呼び出し（旧経路）も生きている
2. WebhookEvents シートを作成（冪等の土台）
3. Cloud Run へデプロイ。LINE Developers の「検証」ボタンで疎通確認
   ・まだ Webhook URL は切り替えない
4. ★切り替え★ LINE Developers の Webhook URL を
   https://jiroku-gateway-xxxxx.run.app/line/webhook へ変更
   ・所要1分。戻すのも1分
   ・切り替え直後に自分のLINEから連携コードを送って往復を確認
5. GAS側の旧経路（body.events を直接処理する分岐）を
   「gatewayLineEvent が有効なら無視する」ようにする
   ・二重処理を防ぐ。旧経路のコード自体は残す
6. 1か月運用し、無事故なら旧経路を削除
```

**切り替えの時間帯**: 連携コードの利用が少ない**平日の午前中**を推奨。
夜間はAIレポート配信と重なるため避ける。

**事前告知**: LINE連携の新規登録が数分止まる可能性があるため、
その時間帯だけ「LINE連携は◯時以降にお願いします」と案内する。
記録・レポートには影響しない。

---

## 15. 2026年8月31日までに完了できるか

**技術的には可能。ただし前提が2つある。**

工数6.5日＋観測1週間に対して、8月1日から31日まで**30日**ある。
比較モード1週間・切り替え後の監視1週間を含めても収まる。

### 前提1: Kaiの作業が3つ必要

いずれも私では実行できない。

| 作業 | 所要 | 理由 |
|---|---|---|
| GCPの課金アカウント有効化 | 15分 | 支払い情報の入力は代行できない |
| LINE Developers で Webhook URL 変更 | 5分 | LINEコンソールへのログインが必要 |
| Secret Manager への値の投入 | 15分 | チャネルシークレットの実物を扱う |

### 前提2: CP3/CP4の切り替えと時期が重なる

CP3（WRITE必須化）とCloud Run移行を**同じ週にやるべきではない**。
どちらも「ログインできない」「記録できない」という形で失敗が出るため、
**同時に動かすと、何が原因か切り分けられなくなる**。

推奨する順序:

```
8月上旬  CP3（WRITE必須化）→ 24時間観測 → CP4（READ必須化）
8月中旬  SESSION_REQUIRED へ
8月中旬〜 Cloud Run 実装（認証の切り替えとは独立して進められる）
8月下旬  /auth/verify を比較モードで1週間
8月末    LINE Webhook 切り替え → gateway モードへ
```

**リスク**: 8月31日は「切り替え完了」の日付としては余裕が無い。
比較モードの1週間で食い違いが見つかると、原因調査で数日ずれる。
**9月上旬まで見ておくのが現実的**で、8月31日を厳守するなら
比較モードを3日に縮める判断が要る（その分、切り替え後の事故確率は上がる）。

---

## 16. この設計で解決しないこと

正直に書いておく。

- **LINEチャネルアクセストークンは当面GAS側に残る。** 送信処理をGASに残すため。
  ゲートウェイへ寄せることもできるが、移行が大きくなるので今回は分離しない
- **ゲートウェイ自身が単一障害点になる。** 特に `/auth/verify` はログインを止める。
  切り替えプロパティで即座に戻せるようにするのが唯一の対策
- **未認証公開の入口が1つ増える。** 署名検証で守るが、
  「誰でも叩けるURL」であること自体は変わらない
- **line_user_id の変更履歴は、この設計では遡れない。** 今後の変更は
  `WebhookEvents` と監査ログに残るが、**過去の不正な書き換えは検出できない**
