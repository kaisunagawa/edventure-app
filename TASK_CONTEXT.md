# タスクの文脈（仕事／プライベート）の扱い

決定: 2026-08-02 / **未実装。Phase 3（Tasksシートを正にする）と同時に入れる**

## 方針

**データは1つ。分けるのは「表示」と「共有」だけ。**

| | |
|---|---|
| 持ち方 | タスクに `context` を1つだけ（仕事 / プライベート / 学び など） |
| 複数タグ | **付けない** |
| 初期値 | 空（未分類）。**勝手に推測して振り分けない** |
| 一覧 | 上部で「すべて / 仕事 / プライベート」を切り替え |
| 既定の表示 | **すべて** |
| 集計 | **常に全部**。分断しない |
| 上司レポート | 仕事タグのものだけ |

## なぜ集計を分けないか

JIROKU は「時間の使い方を全部見て、自己経営を助ける」アプリ。
プライベートを別枠にすると壊れるものがある。

- 睡眠・運動・家族の時間が集計から外れ、**持続可能性が見えなくなる**
- Kaiの3か月目標には「腹筋をシックスパックにする」がある。
  これは仕事かプライベートか、決められない
- 分けた瞬間、片方だけ見て「今日は6時間働いた」と誤判断する

ChatGPTの指示にも「休息・健康・家族を自動的に無駄な時間として減点しない」
「`RECOVERY_RELATIONSHIP` を独立した分類として持つ」とある。
仕事とプライベートを分断すると、この方針と噛み合わない。

## なぜ複数タグにしないか

複数付けられると「仕事でもプライベートでもある」が生まれ、
**集計で二重に数える**か、どちらかを恣意的に選ぶことになる。
ChatGPTの指示「1件の DailyLog には主要分類を1つだけ」と同じ考え方。

## 分ける需要は本物

- 会社のPCで開いたとき、プライベートを見せたくない
- 仕事中に「歯医者の予約」が並ぶと集中が切れる
- 上司へのレポートに私的なタスクを出したくない（既に決まっている要件）

これらは**表示と共有を分ければ足りる**。データを分ける必要はない。

## 実装の順序

★いまの構造には足さない★
タスクは Journal の1列にJSONで入っており、付随情報は「タスク名をキーにした
別マップ」で持っている。ここに `context` を足すと、同じ形のマップが1つ増える。
2026-08-02、この構造が原因で同期の不具合を4回出した。

Tasks シートを正にする Phase 3 で、`context` 列として一緒に入れる。
`sort_order` や `carryover_count` と同じ扱い。

## 端末で分けない

当初「PCと携帯でタスクを変える」案も出たが、採用しない。
端末を変えても同じ分類が使えるほうが素直で、
「同期漏れなのか意図的なのか」が区別できなくなるのを避けるため。
