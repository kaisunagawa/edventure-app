# 招待コードのやり方

JIROKUは招待制です。**コードを発行して渡す**と、その人は自分で始められます。
（コードが無い人は、サインインしても入れません）

---

## 1. コードを発行する

```bash
bash gas/ops.sh inviteCreate label=trial maxUses=5 days=30
```

| 指定 | 意味 | 目安 |
|---|---|---|
| `label` | 何のコードか（自分用のメモ） | **英数字だけ**。日本語は不可 |
| `maxUses` | 何人まで使えるか | 個人に渡すなら `1`、募集なら `20` など |
| `days` | 何日で切れるか | 既定30。長くしすぎない |

**★label に日本語を入れないこと★** 署名が壊れて `AUTH_REQUIRED` になります。

返ってくるもの:

```json
{"ok":true,"code":"JRK-G63N-FAKZ","maxUses":5,
 "expiresAt":"2026-10-03T04:43:25.439Z",
 "url":"https://kaisunagawa.github.io/edventure-app/?invite=JRK-G63N-FAKZ"}
```

**`url` をそのまま渡すのがいちばん親切です。**開いた時点でコードが入っています。

---

## 2. 渡す（LINE用・1行14文字以内）

```
JIROKUの招待です

下のリンクを開いて
Googleでサインイン

https://kaisunagawa.github.io/edventure-app/?invite=JRK-XXXX-XXXX

・コードは入力済み
・スマホ推奨
・Gmailだとすぐ入れます
・最初に3か月の目標を
　1つ決めます
```

コードだけ伝える場合は「ログイン画面の**招待コードをお持ちの方**を押して、
コードを入れてからサインイン」と添えること。

---

## 3. 相手の画面で起きること

1. リンクを開く → コードが入った状態のログイン画面
2. **Googleでサインイン**を押す
3. そのまま中に入れる（Usersの行は自動で作られる）
4. 質問に答えて、3か月後どうなりたいかを1つ決める

うまく行かないときは、たいてい次のどれかです。

| 症状 | 原因 | どうする |
|---|---|---|
| 「まだ登録されていません」 | コードが入っていない | 画面下の欄に入れてもらう |
| 何度やっても入れない | コードが尽きた／期限切れ／停止済み | `inviteList` で確認して出し直す |
| Gmail以外で止まる | ドメインの持ち主を確認できない | Gmailで入り直してもらう |

---

## 4. 確認する・止める

```bash
bash gas/ops.sh inviteList
```

```bash
bash gas/ops.sh inviteRevoke code=JRK-XXXX-XXXX
```

`used` が `max` に達したコードは、それ以上使えません。
外に出しすぎたと思ったら、**すぐ `inviteRevoke` で止める**こと。
止めても、すでに入った人には影響しません。

---

## 仕組み（触るとき用）

- コードの表 … スプレッドシートの `InviteCodes`
- 入口 … `redeemInvite`（`login` とまったく同じ検証を通る）
- **行を作るメールは、必ずGoogleが検証したもの**。リクエストのメールは見ない。
  ここを変えると、2026-08-01に塞いだ穴が元に戻る
- 発行系は `ADMIN_SECRET_ALLOWLIST` に載っているものだけ鍵で叩ける
- 誰の招待で入ったかは `Users.invited_by_code` に残る
