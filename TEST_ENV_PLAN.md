# テスト環境の分離 ─ 設計と見積もり

作成: 2026-08-01 / **状態: 設計のみ。スプレッドシートも発行処理も作っていない**

---

## 1. なぜ必要か

所有権テストのうち **「有効なセッションを持った他人になりすます」系が自動化できない。**
平文のセッショントークンは保存していないので、テストのたびに
実際のGoogleログインを踏む必要がある。

2026-07-31 はKaiのログイン中に手作業で確認したが、**回帰テストとして繰り返せない**。
繰り返せないテストは、次の変更で壊れても気づけない。
実際、`forceSelfEmail` の配線漏れは「実装したのに効いていない」状態で、
コードレビューでは見つからなかった。

一方で「本番から到達できるテスト用セッション発行口」は作らない。
管理鍵1本で任意のユーザーになりすませる穴になるため。

**答え: 本番データに触れない、独立したテスト環境を作る。**

---

## 2. いまの構成の問題

```
本番デプロイ  ─┐
              ├→ 同じスプレッドシート（本番データ）
検証デプロイ  ─┘
```

「検証環境」と呼んでいるが、**見ているデータは本番**。
だから検証環境で書き込みテストができない。今回も
`saveLog` を確認のつもりで叩いて本番に空の記録を作ってしまった。

---

## 3. 分離後の構成

```
本番デプロイ    → 本番スプレッドシート    environment=PROD
検証デプロイ    → 本番スプレッドシート    environment=TEST   ← 現状維持（読み取り検証用）
★テストデプロイ → テスト用スプレッドシート environment=SANDBOX ← 新設
```

3つ目を足す。既存2つには手を入れない。

| 項目 | 設定 |
|---|---|
| スプレッドシート | **新規**。本番とは別ID。構造だけコピーし、データは入れない |
| ユーザー | 架空のテストユーザーのみ（`test-a@example.invalid` 等） |
| セッション秘密鍵 | 本番と**別**（`SESSION_TOKEN_SECRET_SANDBOX`） |
| deployment_id | 本番・検証と別 |
| environment | `SANDBOX` |
| 有効期限 | テストセッションは**15分以内** |
| 後始末 | テスト終了時に自動失効・削除 |
| 監査 | AuthAudit へ `environment=SANDBOX` として記録 |

**`.invalid` ドメインを使う理由**: RFC 2606 で「絶対に実在しない」と
予約されている。実在するかもしれないアドレスを架空ユーザーに使うと、
テストのつもりで実在の誰かへメールが飛ぶ事故が起きうる。

---

## 4. 本番でTESTセッションを必ず拒否する

これが最重要。3重にする。**1つでも通れば穴になる**ので、
「どれか1つ」ではなく「すべて」を実装する。

```
① 鍵が違う
   SANDBOXのセッションは SESSION_TOKEN_SECRET_SANDBOX で作られる。
   本番の鍵では検証が通らない。

② セッション行に environment 列を持たせる
   verifySession が currentEnvironment() と突き合わせ、
   一致しなければ拒否する（環境不一致・deployment不一致の検出）。
   ※ この列は現在存在しない。追加が必要。

③ テスト用の発行処理を本番デプロイに含めない
   Apps Script の内部関数として置き、doGet/doPost の switch に載せない。
   ★外部から到達できるアクションにはしない★
```

**②はCP3/CP4にも効く。** ChatGPTが挙げた拒否条件のうち
「environment不一致」「deployment不一致」は、
現在の `verifySession` が**見ていない**（列が無いため）。
ここを足すと、その2条件も同時に満たせる。

### 発行処理の置き場所

```javascript
// ★doGet/doPost からは絶対に呼べない位置に置く★
// Apps Scriptエディタから手で実行するか、テスト専用トリガーからのみ呼ぶ。
// SANDBOX環境でなければ即座に例外を投げる（fail-closed）。
function __sandboxIssueSession(testUserId) {
  if (currentEnvironment() !== "SANDBOX") throw new Error("SANDBOX以外では発行しない");
  ...  // 有効期限15分
}
```

関数名を `__` で始めるのは、`case "..."` の一覧に紛れ込ませないため。
スモークテストに **「`__sandbox` で始まる関数が switch に載っていないこと」**
を検査する項目を足す。

---

## 5. 自動テストで実行する項目

USER A と USER B の2人を架空で作り、**Aのセッションで**次を試す。
すべて拒否されることを確認する。

| # | 試すこと | 期待 |
|---|---|---|
| 1 | Bの `studentEmail` を指定 | Aへ強制される（forceSelfEmail） |
| 2 | Bの `log_id` を取得 | 拒否 |
| 3 | Bの `log_id` を更新 | 拒否 |
| 4 | Bの `log_id` を削除 | 拒否 |
| 5 | Bの `task_id` | 拒否 |
| 6 | Bの `goal_id` | 拒否 |
| 7 | Bの `weekly_goal_id` | 拒否 |
| 8 | Bの `report_id` | 拒否 |
| 9 | Bの `journal_id` | 拒否 |
| 10 | Bの `file_id` | 拒否 |
| 11 | Bの `coaching_note_id` | 拒否 |
| 12 | COACHが担当外ユーザーを指定 | 拒否 |
| 13 | **SANDBOXセッションを本番URLへ送信** | 拒否 |

13番目が分離の要。これが通ってしまったら、テスト環境が
本番への抜け道になっている。

### 「拒否」の判定を厳密にする

今回のスモークテストで、応答が空なのを「拒否」と誤読して
正常なデプロイを止めた。同じ間違いを繰り返さないため、

```
拒否   = ok:false が返る
判定不能 = 応答が空 / タイムアウト  ← ★合格にしない★
```

を分けて数える（`ownership_test.sh` で既に採用済みの方式）。

---

## 6. 見積もり

| 作業 | 目安 |
|---|---|
| テスト用スプレッドシートの作成と構造コピー | 0.5日 |
| Sessions に `environment` 列を追加し、verifySession で突き合わせ | 0.5日 |
| SANDBOX判定・専用鍵・`__sandboxIssueSession` | 0.5日 |
| 架空ユーザーの投入と後始末（自動失効・削除） | 0.5日 |
| 13項目の自動テスト | 1日 |
| 本番でSANDBOXセッションが拒否されることの実測 | 0.5日 |
| **合計** | **約3.5日** |

**費用: 0円**（スプレッドシートとデプロイが増えるだけ）

### 注意点

- **GASのデプロイ枠**: バージョンは200個まで。7月31日に上限へ達した実績がある。
  デプロイを1つ増やすとバージョン消費が増えるので、
  `push_gas.sh` の「1回のデプロイで1バージョン」を維持する
- **`environment` 列の追加は既存セッションに影響する。**
  列が空の既存行をどう扱うかを決める必要がある。
  **空を「PROD」とみなす**（fail-openになるが、既存利用者を締め出さない）か、
  **空を拒否する**（fail-closedだが、全員が再ログインになる）かの判断が要る。
  → CP3の切り替えと同時にやれば、どのみち再ログインするので
    **fail-closedにできる**。タイミングを合わせるのが得

---

## 7. 今回やらないこと

ChatGPTの指示どおり、**設計と見積もりまで**。

- 新しいスプレッドシートは作らない
- 発行処理は書かない
- `environment` 列も追加しない

本番の最終受け入れ確認は、Kaiの正規セッションを使って手動で行う。
